import { GrammarQuiz } from "@/components/train/grammar-quiz";

export const metadata = {
  title: "Grammar — 家有儿女 EP5",
  description: "Pick the grammar pattern used in an EP5 worked example",
};

export default function GrammarPage() {
  return <GrammarQuiz />;
}
