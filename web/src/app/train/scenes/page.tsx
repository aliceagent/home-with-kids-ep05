import { ScenesQuiz } from "@/components/train/scenes-quiz";

export const metadata = {
  title: "Scene order — 家有儿女 EP5",
  description: "Put four consecutive EP5 lines back in spoken order",
};

export default function ScenesPage() {
  return <ScenesQuiz />;
}
