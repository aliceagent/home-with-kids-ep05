import { CultureQuiz } from "@/components/train/culture-quiz";

export const metadata = {
  title: "Culture notes — 家有儿女 EP5",
  description: "Quiz the EP5 culture sidebars: 阿姨, F4, 大片, and 作文",
};

export default function CulturePage() {
  return <CultureQuiz />;
}
