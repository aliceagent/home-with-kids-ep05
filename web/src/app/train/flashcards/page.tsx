import { FlashcardsSession } from "@/components/train/flashcards-session";

export const metadata = {
  title: "Flashcards — 家有儿女 EP5",
  description: "Spaced-repetition flashcards for EP5 vocabulary and idioms",
};

export default function FlashcardsPage() {
  return <FlashcardsSession />;
}
