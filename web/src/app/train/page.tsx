import { TrainHub } from "@/components/train/train-hub";

export const metadata = {
  title: "Training — 家有儿女 EP5",
  description:
    "Listening, matching, flashcards, speaker ID, and tone drills for Home With Kids EP5",
};

export default function TrainPage() {
  return <TrainHub />;
}
